# Nancy.Plots.Terminal

Terminal plotting backend for Nancy curves and sequences.

The package renders compact character-grid plots and uses Spectre.Console so ANSI colors are emitted only when the target console supports them.
